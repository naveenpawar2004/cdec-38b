pipeline {
   agent any 
    stages {
      stage ('pull-stage') {
       steps {
          echo "pull new job"
       }
      }
      stage ('build-stage') {
       steps {
            echo "build new job"
       }
      }
    }
}
